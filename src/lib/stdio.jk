class string {
    char[] data;
    int length;

    func char __index(int n) {
        // ERRORS
        ret this.data[n];
    }
}

func void print(string input) {
    for (int i = 0; i < input.length; i++) {
        write(input[i]);
    }
}
